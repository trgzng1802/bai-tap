#include <iostream>
using namespace std;
auto function(int a, int b)
{
    return a + b;
}
int main()
{
    //auto c = 'g';
    //cout << typeid(c).name() << endl;
   //auto i = 9;
    //cout << typeid(i).name() << endl;
    //auto d = 5.5;
    //cout << typeid(d).name() << endl;
    //auto f = 1.0F;
    //cout << typeid(f).name() << endl;
    //auto function = (4,5);
    //cout << "Type of function: " << typeid(function).name();
    return 0;
}